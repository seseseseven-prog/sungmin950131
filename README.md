# 메모 & 체크리스트 앱

브라우저에서 바로 실행 가능한 간단한 노트패드 + 체크리스트 앱입니다.

## 기능
- 메모 자동 저장/복원 (localStorage)
- 체크리스트 항목 추가/완료/삭제
- 완료 개수 표시

## 실행 방법
정적 파일이라서 `index.html`을 브라우저로 열면 바로 사용할 수 있습니다.

간단한 서버로 실행하려면:

```bash
python3 -m http.server 8000
```

그다음 브라우저에서 `http://localhost:8000` 접속.
